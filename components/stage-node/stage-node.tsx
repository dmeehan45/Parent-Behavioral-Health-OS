"use client";
import Link from "next/link"; import { Handle, Position, type NodeProps } from "@xyflow/react";
export type StageNodeData={id:string;title:string;order?:number;status?:string;summary?:string;steps:number;claims:number;questions:number;bets:number;prototypes:number;selected?:boolean};
export function StageNode({data}:{data:StageNodeData}&NodeProps){return <div className={`flow-node stage-flow-node ${data.selected?"selected":""}`}><Handle type="target" position={Position.Left}/><div className="node-top"><span className="node-index">{String(data.order??"").padStart(2,"0")}</span><span className="node-status">{data.status??"unmapped"}</span></div><strong className="node-title">{data.title}</strong><p>{data.summary}</p><div className="counts"><span><b>{data.steps}</b> steps</span><span><b>{data.questions}</b> questions</span>{data.bets>0&&<span className="count-accent"><b>{data.bets}</b> bets</span>}{data.prototypes>0&&<span className="count-prototype"><b>{data.prototypes}</b> prototype</span>}</div><Link className="node-link" href={`/stages/${data.id}`} onClick={event=>event.stopPropagation()}>Open stage <span>→</span></Link><Handle type="source" position={Position.Right}/></div>}
