import Link from "next/link";import type {Bet} from "@/lib/schemas";import {SECTION} from "@/lib/content/body";export function BetCard({bet}: {bet:Bet}){return <Link href={`/bets/${bet.id}`} className="panel" style={{textDecoration:"none",display:"block"}}><span className="eyebrow">Bet · {bet.confidence??"unrated"} confidence</span><h3 style={{marginTop:10}}>{bet.title}</h3><p className="muted">{bet.sections[SECTION.bet]}</p><span className="explore">Review bet →</span></Link>}
